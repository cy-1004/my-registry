{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "anime-text-bounce",
  "title": "Anime Text Bounce",
  "description": "Per-character bounce-in built with anime.js splitText and staggered keyframes.",
  "dependencies": [
    "animejs"
  ],
  "files": [
    {
      "path": "registry/ui-animations/anime-text-bounce/anime-text-bounce.tsx",
      "content": "\"use client\"\n\nimport * as React from \"react\"\nimport { animate, splitText, stagger } from \"animejs\"\n\nimport { cn } from \"@/lib/utils\"\n\ninterface AnimeTextBounceProps {\n  children: string\n  className?: string\n  loop?: boolean\n  loopDelay?: number\n}\n\nexport function AnimeTextBounce({\n  children,\n  className,\n  loop = true,\n  loopDelay = 1000,\n}: AnimeTextBounceProps) {\n  const ref = React.useRef<HTMLHeadingElement>(null)\n\n  React.useEffect(() => {\n    const element = ref.current\n    if (!element) return\n\n    const { chars, revert } = splitText(element, {\n      words: false,\n      chars: true,\n    })\n\n    const animation = animate(chars, {\n      y: [\n        { to: \"-2.75rem\", ease: \"outExpo\", duration: 600 },\n        { to: 0, ease: \"outBounce\", duration: 800, delay: 100 },\n      ],\n      rotate: {\n        from: \"-1turn\",\n        delay: 0,\n      },\n      delay: stagger(50),\n      ease: \"inOutCirc\",\n      loopDelay,\n      loop,\n    })\n\n    return () => {\n      animation.revert()\n      revert()\n    }\n  }, [children, loop, loopDelay])\n\n  return (\n    <h2 ref={ref} className={cn(className)}>\n      {children}\n    </h2>\n  )\n}\n",
      "type": "registry:ui",
      "target": "@components/ui-animations/anime-text-bounce.tsx"
    }
  ],
  "categories": [
    "ui-animations"
  ],
  "type": "registry:ui"
}